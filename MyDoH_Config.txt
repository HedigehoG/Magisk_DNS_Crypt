# DNS_CHOICE=4 # Свой вариант
# CUSTOM_DNS_HOST=https://my.example.com/dns-query # Свой вариант
# CUSTOM_BOOTSTRAP_IP=1.2.3.4,5.6.7.8 # Опционально: IP-адреса для начального разрешения домена, через запятую. Если не указано, будут использоваться IP по умолчанию (1.1.1.1,1.0.0.1).

# DNS_CHOICE=1 # Деф клоуд
# DNS_CHOICE=2 # Гугел
# DNS_CHOICE=3 # Сомсс

# DNS_CHOICE=4 # Свой вариант nextdns
# CUSTOM_DNS_HOST=https://dns.nextdns.io/NEXTDNS_CONFIG_ID
# CUSTOM_BOOTSTRAP_IP="45.90.28.0,45.90.30.0" # Standard NextDNS IPs

# DNS_CHOICE=4 # Свой вариант AdGuard DNS
# CUSTOM_DNS_HOST=https://dns.adguard.com/dns-query/ID
# CUSTOM_BOOTSTRAP_IP="94.140.14.14,94.140.14.15" # Standard NextDNS IPs
